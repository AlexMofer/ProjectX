apply plugin: 'com.android.library'

android {
    compileSdkVersion 28

    defaultConfig {
        minSdkVersion 4
        targetSdkVersion 28
        versionCode 6
        versionName "2.0.2"
    }
}

//apply from: "bintray.gradle"