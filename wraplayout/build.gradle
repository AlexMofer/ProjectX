apply plugin: 'com.android.library'

android {
    compileSdkVersion 26

    defaultConfig {
        minSdkVersion 4
        targetSdkVersion 26
        versionCode 4
        versionName "1.2.0"
    }
}

//apply from: "bintray.gradle"